#include<stdio.h>
int main()
{
	char str1[50],str2[50];
	scanf("%s",str1);
	scanf("%s",str2);
	int i=strcmp(str1,str2);
	if(i==0)
	{
		printf("string are equal");
	}
	else
	{
	printf("string are not equal");
}
	
}
