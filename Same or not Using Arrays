#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
 int n,m;
    scanf("%d%d",&n,&m);
    int a[n],b[m],i;
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        
    }
    for(i=0;i<m;i++)
    {
        scanf("%d",&b[i]);
    }
      if(n!=m)
    {
          printf("Not Same");
      }
    else
    {
        for(i=0;i<n;i++)
        {
        if(a[i]!=b[i])
        {
            printf("Not Same");
             break;
          }
    }
    if(i==n)
        printf("Same");
    }
    return 0;
}


