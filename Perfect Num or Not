#include <stdio.h>

int main() {
    int i,n,c;
    scanf("%d",&n);
    for(i=1;i<n;i++)
    {
    if(n%i==0)
    c=c+i;
    }
    if(c==n)
    printf("perfect num");
    else
    printf("Not a perfect num");
    return 0;
}
